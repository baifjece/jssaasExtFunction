临时JS函数文件夹，该文件夹下函数，在经过一定的版本后，会在jssaas底层实现，提高执行效率

在jsfun文件夹下创建对应的文件ext_Demo.js，并实现ext_Demo.js内的函数,函数以_ext_开头，如_ext_Demo_xxx(),
在jsfun文件夹下创建对应的文件ext_Demo.js，并新增fnDemo.js,实现ext_Demo内的函数。

此扩展机制也可以使用在替换底层函数，如：jssaas实现的函数达不到开发者开发需要，可以借此机制，替换底层函数。



